OPENAI_API_KEY=
# Any origin unless set to one. A value must be a single origin, not a list.
CORS_ORIGIN=*

# Host port the proxy is published on, loopback only. Caddy listens on 8082
# inside the container whatever this says.
EMBEDDINGS_PORT=8082
